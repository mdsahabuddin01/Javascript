// how many vowels in a sentence

// const vowels = [1, 2, 3, 3, 4, 5, 5, 6, 7, 8, 9, 10];

let age = "Test";

age = Number(age);

if (isNaN(age)){
    console.log("Input is not a number");
} else {
    console.log(age < 18? "Too young":"Old enough");
}
