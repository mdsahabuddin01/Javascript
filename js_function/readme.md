Function in Javascript are reusable block of code that are designed to perform specific tasks. It allows to reuse, organise, and modularize codes. It can take inputs, perform actions, return outputs.
